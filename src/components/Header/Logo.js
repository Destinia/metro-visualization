import React from 'react';

import styles from './Logo.module.css';

const Logo = () => (
  <div className={styles.title}>
    <span className={styles.logo}>Realtime Trace</span>
  </div>
);

export default Logo;
